require('ufo').setup {
    open_fold_hl_timeout = 0,
    preview = {
        win_config = {
            border = 'single',
            winblend = 0,
            winhighlight = 'Normal:Normal'
        },
    }
}
